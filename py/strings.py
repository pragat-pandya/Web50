name = "Harry Potter"
# Strings can be logically seen as sequences of characters rather than an individual object!

# We can acess individual elements of strings using arry like subscripts [].
# and obviously the first index is 0 and not 1
print (name[0])
print (name[1])
print (name[2])

# Strings in python: Ordered-Yes, Mutable-No
